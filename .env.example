# OCR.space API Key (Required)
# Get your free API key from https://ocr.space/ocrapi
OCRSPACE_API_KEY=your_api_key_here

# Optional: Skip heavy model loading on free tier (set to true for Railway free tier)
SKIP_MODEL_LOADING=false

# Optional: Port (Railway sets this automatically)
PORT=8080
